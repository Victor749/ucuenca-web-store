server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Victor749/ucuenca-web-store.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: kalkstein
      password: Th3_eth3r
